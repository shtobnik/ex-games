import React, { FC } from 'react';

// Styles
import styles from './LanguageSwitcher.module.scss';

const LanguageSwitcher: FC = () => (
  <div className={styles.languageSwitcher}>Language</div>
);

export default LanguageSwitcher;
